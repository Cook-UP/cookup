import React from "react";
import Menu from "../components/Menu/Menu";
// Renders the Menu component to the webpage
export const DisplayMenu = () => {
  return (
    <div>
      <Menu></Menu>
    </div>
  );
};

export default DisplayMenu;
